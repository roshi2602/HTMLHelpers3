@model HTMLHelpers3.Models.Login

@Html.Password("login password")
<!--PasswordFor requires lambda expression-->
type password-@Html.PasswordFor(x => x.LoginPassword)

<!--
using HiddenField helper-used to store hidden values on webpage

HiddenFor() method is the lambda expression which specifies the model property to be bind with the hidden field.
-->
passowrd- @Html.HiddenFor(x => x.LoginPassword);